{
  "name": "ozonwbapps",
  "version": "0.1.0",
  "private": true,
  "description": "Поиск выгодных предложений на маркетплейсах (Ozon, Wildberries и др.)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "start": "node scripts/dev.mjs",
    "stop": "docker compose stop",
    "wb:diag": "node --env-file=.env scripts/wb-diag.mjs",
    "diag": "node scripts/diag.mjs",
    "build:shared": "npm run build --workspace @ozonwb/shared",
    "build": "npm run build:shared && npm run build --workspace @ozonwb/api && npm run build --workspace @ozonwb/worker && npm run build --workspace @ozonwb/web",
    "dev:api": "npm run dev --workspace @ozonwb/api",
    "dev:worker": "npm run dev --workspace @ozonwb/worker",
    "dev:web": "npm run dev --workspace @ozonwb/web",
    "db:generate": "node --env-file=.env node_modules/prisma/build/index.js generate --schema packages/db/prisma/schema.prisma",
    "db:migrate": "node --env-file=.env node_modules/prisma/build/index.js migrate deploy --schema packages/db/prisma/schema.prisma",
    "db:migrate:dev": "node --env-file=.env node_modules/prisma/build/index.js migrate dev --schema packages/db/prisma/schema.prisma",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\""
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
